# Copy to .env and adjust. Consumed by docker compose.

# Host port to expose the app on (container always listens on 3000).
WEB_PORT=3000

# Canonical site URL for SEO (sitemap, OG tags). Use the real deployed URL.
NUXT_PUBLIC_SITE_URL=https://jcicr.cz
